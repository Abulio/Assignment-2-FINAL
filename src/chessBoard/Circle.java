package chessBoard;

public class Circle extends Shapes
{
    Integer radius;//radius of circle not set yet
    public Circle(Integer x, Integer y, Integer radius)
    {
        this.radius = radius;//radius of circle
        this.posX = x;//position of x
        this.posY = y;//position of y
    }
}
